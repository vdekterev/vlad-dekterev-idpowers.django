import argparse

parser = argparse.ArgumentParser(description='A tutorial of argparse!')
parser.add_argument("--name", required=True, type=str, help="Enter a name")
args = parser.parse_args()

print(f'Hello, World! I am {args.name}'!)
